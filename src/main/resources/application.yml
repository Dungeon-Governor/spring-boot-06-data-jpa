spring:
  datasource:
    url: jdbc:mysql://localhost/mybatis?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
#      设置数据库的生成策略为，更新和创建数据表结构
      ddl-auto: update
#      在控制台显示执行的sql
    show-sql: true